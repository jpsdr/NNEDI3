The model weights for nnedi3 are stored in a binary file available at:

  http://bengal.missouri.edu/~kes25c/binary1.bin

The project includes it as a resource. It isn't included here to
keep the size of the zip file down.